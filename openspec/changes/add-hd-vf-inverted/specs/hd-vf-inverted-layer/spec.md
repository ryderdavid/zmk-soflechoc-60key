## ADDED Requirements

### Requirement: VF^ layer with swapped rows
Layer 11 SHALL be VF with top and bottom alpha rows swapped. Display "hd-Vf^".

#### Scenario: VF^ top row has VF's bottom row letters
- **WHEN** the user activates VF^
- **THEN** top row produces F P L D V and right top produces -+ U O Y B
