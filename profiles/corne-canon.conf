# Corne-Canon Profile — Kconfig overrides
# Increased combo limit for dual-layout word chords
# Each space key participates in ~100 word chord combos + utility combos
CONFIG_ZMK_COMBO_MAX_COMBOS_PER_KEY=110
